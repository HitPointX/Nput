# Cross-compilation linker configuration.
# Tells cargo to use the mingw-w64 toolchain when targeting Windows.
# Requires mingw-w64-gcc to be installed (pacman -S mingw-w64-gcc).

[target.x86_64-pc-windows-gnu]
linker = "x86_64-w64-mingw32-gcc"
ar     = "x86_64-w64-mingw32-ar"
